Place the built installer here as:

    GenNal-Setup.exe

It is produced by Phase 8 of PLAN.md:

    npm run build
    npx electron-builder         # outputs dist/GenNal Setup.exe

Copy/rename that file to this folder (GenNal-Setup.exe) and the website's
"Download for Windows" buttons will serve it.
